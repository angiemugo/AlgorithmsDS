import UIKit

public func solution(_ A : inout [Int]) -> Int {
// check if number is smaller than zero, if so break
    // if not, check if number is bigger than next 
    for i in A {
        guard i > 0 else { break }

    }
}
